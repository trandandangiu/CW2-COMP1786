<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MHike"
        tools:targetApi="31">

        <!-- Activity chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity thêm Hike -->
        <activity
            android:name=".AddHikeActivity"
            android:exported="false" />

        <!-- 🟢 Activity thêm Observation -->
        <activity
            android:name=".AddObservationActivity"
            android:exported="true" />

        <!--🟢 Activity thêm SearchHikeActivity -->
        <activity
            android:name=".SearchHikeActivity"
            android:exported="true" />
        <activity android:name=".ViewObservationsActivity"
            android:exported="true"/>
        <activity
            android:name=".EditObservationActivity"
            android:exported="true" />


    </application>

</manifest>
